export const DB_NAME = "videotube"

// 🧠 What This Line Means

// You are defining a constant database name

// Exporting it so it can be reused anywhere in the project

// Prevents hard-coding the DB name in multiple places



// 🎯 Interview-Ready Explanation

// “We keep the database name in a constant to avoid duplication, improve maintainability, and follow the single-source-of-truth principle.”